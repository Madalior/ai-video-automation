# Clipper — AI Video Repurposing Tool
# Powered by NVIDIA NIM + faster-whisper + FFmpeg

# ── Core AI ─────────────────────────────────────────────────
openai>=1.0.0              # NVIDIA NIM uses OpenAI-compatible API
faster-whisper>=1.0.0      # Local transcription, word timestamps, free

# ── Video ───────────────────────────────────────────────────
yt-dlp>=2026.7.0           # Download any video (YouTube, etc.)

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0.0       # Load .env config
requests>=2.31.0           # HTTP requests
rich>=13.0.0               # Beautiful terminal output
tqdm>=4.66.0               # Progress bars

# ── Optional: Face tracking / smart crop ────────────────────
# opencv-python>=4.8.0     # Uncomment for smart speaker tracking
# mediapipe>=0.10.0        # Uncomment for face detection
